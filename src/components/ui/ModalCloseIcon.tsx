import { X } from "lucide-react";

export function ModalCloseIcon() {
  return (
    <X
      size={15}
      strokeWidth={2.4}
      strokeLinecap="square"
      absoluteStrokeWidth
      aria-hidden="true"
    />
  );
}
